// Pre-rendering is disabled due to root layout's browser-specific dependencies
// These pages will be client-rendered via the SPA fallback (200.html)
export const prerender = true;
export const ssr = false;
