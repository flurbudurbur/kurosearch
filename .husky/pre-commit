# prettier formatting
pnpm lint:fix
# Pass --fix to let ESLint auto-fix where possible.
pnpm lint --fix

# Re-stage any files modified by the formatter/linter so fixes are included in the commit.
git add -A
